# string datatype pattern with regex
```jsbp
String(pattern="[a-z][0-9]*[a-z]")
```

## string matching the regex
```json
"n00b"
```
+ Valid

## string that doesn't match the regex
```json
"noob"
```
+ Invalid
    - `.`